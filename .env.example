# ============================================================================
# SmartLens OCR Frontend - Environment Variables
# ============================================================================
# Copy this file to .env.local and update values as needed
# Do NOT commit .env.local to version control

# ============================================================================
# Backend Configuration
# ============================================================================

# Production Backend (Google Cloud Run)
BACKEND_URL=https://smartlensocrv2-bk-sp62tbdjjq-uw.a.run.app

# Development Backend (Local)
# BACKEND_URL=http://localhost:8000

# Staging Backend
# BACKEND_URL=https://backend-staging.your-domain.com

# ============================================================================
# Node Environment
# ============================================================================

# Set to 'development' for local development
# Set to 'production' for deployed instances
NODE_ENV=development

# ============================================================================
# Next.js Configuration (Optional)
# ============================================================================

# Disable telemetry in development (recommended)
NEXT_TELEMETRY_DISABLED=1

# Debug API requests (only in development)
# DEBUG=*

# ============================================================================
# Notes
# ============================================================================
# - All changes to this file only affect local development
# - For production deployment to Cloud Run, set env vars via gcloud:
#   gcloud run services update SERVICE_NAME \
#     --set-env-vars BACKEND_URL=https://your-backend-url
# - Never commit sensitive environment variables
# - Use .gitignore to exclude .env.local

